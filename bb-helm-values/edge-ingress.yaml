apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    dev.okteto.com/generate-host: "true"
  name: edge-ingress
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: edge
          servicePort: 8080
        path: /